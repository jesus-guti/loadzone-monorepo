# DD-04 — Admin data surface prototype (throwaway)

Static HTML + Tailwind CDN. Answers: does the locked admin experience feel right on a concrete Wellness list?

## Run

```bash
cd .scratch/dd-04-admin-prototype/prototype && python3 -m http.server 8765
```

Open [http://localhost:8765](http://localhost:8765).

## Scope

- Desktop shell: primary nav + Más (secondary) + team `Juvenil A`
- Invisible Wellness roster list (exercise-library pattern)
- One risk callout card (does not wrap the list)
- Spanish UI copy; mock data in memory

Not production. Do not promote into `apps/app`.
