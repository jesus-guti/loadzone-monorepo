# Prototype TCG frame and working tilt on the real Streak Cromo

Type: prototype  
Status: resolved  
Claimed by: me  
Blocked by: 01, 02

## Question

On the **judge surface from ticket 01**, does a throwaway change to the production Streak Cromo (TCG-like frame, pointer 3D tilt that actually moves, foil only on chrome, matte portrait) feel like a collectible card — or still like the current rounded chrome slab?

## Answer

**Collectible.** Pointer 3D tilt on the real Racha-sheet cromo is unmistakable (modest turn, desktop mouse). Start production from **A — Bezel clásico** (thick rectangular bezel, arched portrait window). B (stadium plate) and C (paspartú) lose. Do not judge via `?variant=` or `prototype-cromo-foil`. Throwaway lives at `apps/player/app/[token]/prototype-cromo-tcg/` and mounts in the Racha sheet when `?cromo=A|B|C` (dev only).
