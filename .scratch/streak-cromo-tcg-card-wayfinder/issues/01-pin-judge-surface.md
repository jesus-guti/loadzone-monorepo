# Pin how a human judges the Streak Cromo

Type: grilling  
Status: resolved  
Blocked by:

## Question

How does a maintainer open and judge the **production** Streak Cromo (Racha sheet and/or today’s completion) so tilt and TCG frame are unmistakable — without `?variant=` DD-05, and without a parallel fake check-in form?

## Answer

**Primary judge surface = Racha sheet only.** Open any seed **Player** at `/[token]` with **no** `?variant=` (and no lab auto-open query). Tap the header Recoverable Streak pill — same gesture as a player. Judge the production `StreakCromo` inside that sheet on **desktop with mouse**: pointer tilt over the card is the acceptance signal. Today’s check-in completion cromo is **not** the judge surface for this map (it may still ship the same component later). Never use DD-05 (`?variant=`) or `prototype-cromo-foil` to decide.
