# wayfinder:task

## Question

Create `apps/app/components.json` as a consumer workspace config.

Based on research ticket #01, create the file with:
- Correct aliases pointing to `@repo/design-system/components` for UI components
- Local aliases (`@/`) for app-specific components
- Same `style`, `iconLibrary`, and `baseColor` as the design-system config

Also create `apps/player/components.json` with the same structure.
