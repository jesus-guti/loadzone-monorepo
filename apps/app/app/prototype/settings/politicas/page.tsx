import type { Metadata } from "next";
import { SettingsStubPage } from "../_components/settings-stub-page";

export const metadata: Metadata = {
  title: "Políticas · Prototipo configuración | LoadZone",
};

const PoliticasStubPage = () => <SettingsStubPage title="Políticas" />;

export default PoliticasStubPage;
