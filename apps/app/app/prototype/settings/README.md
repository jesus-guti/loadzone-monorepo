# Settings shell prototype (JES-57)

Throwaway UI: `pnpm --filter app dev` → open [http://localhost:3000/prototype/settings](http://localhost:3000/prototype/settings) (signed-in staff). Redirects to Equipo.
