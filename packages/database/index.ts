/**
 * Default `@repo/database` entry: Prisma client (`database`), generated types, risk helpers.
 * For seed-like upserts (e.g. system form templates), import `@repo/database/bootstrap` explicitly.
 */
export * from "./client";
